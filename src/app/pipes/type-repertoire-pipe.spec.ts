import { TypeRepertoirePipe } from './type-repertoire-pipe';

describe('TypeRepertoirePipe', () => {
  it('create an instance', () => {
    const pipe = new TypeRepertoirePipe();
    expect(pipe).toBeTruthy();
  });
});
