import { TypePersonnelPipe } from './type-personnel-pipe';

describe('TypePersonnelPipe', () => {
  it('create an instance', () => {
    const pipe = new TypePersonnelPipe();
    expect(pipe).toBeTruthy();
  });
});
