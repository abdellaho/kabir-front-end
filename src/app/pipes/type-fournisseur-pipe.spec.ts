import { TypeFournisseurPipe } from './type-fournisseur-pipe';

describe('TypeFournisseurPipe', () => {
  it('create an instance', () => {
    const pipe = new TypeFournisseurPipe();
    expect(pipe).toBeTruthy();
  });
});
